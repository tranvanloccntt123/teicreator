import { scale } from "react-native-size-matters";

export const BTN_OPTION_ICON_SIZE = scale(9);
export const BTN_OPTION_SIZE = scale(18);
export const MIN_SCALE = 0.3;
export const MAX_SCALE = 3.0;
